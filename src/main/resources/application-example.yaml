spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yasn_db
    username: your username
    password: your password
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: update


yasn:
  http:
    request:
      path: "src/main/resources/songs_normalize.csv"